# configurações de banco
DB_NAME=ponskan
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
# configurações da API
PORT=8080
NODE_ENV=development
# segurança (JWT)
# gerar via: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_secreta
JWT_EXPIRES_IN=1h